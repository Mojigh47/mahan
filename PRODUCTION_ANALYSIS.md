# گزارش آنالیز بی‌رحمانه برای پروداکشن (پروژه ماهان)

این گزارش شامل بررسی دقیق و سخت‌گیرانه کدهای پروژه برای اطمینان از آمادگی ۱۰۰٪ جهت انتشار در محیط پروداکشن است.

## ۱. شکاف‌های امنیتی بحرانی (Critical Security Gaps)

### الف) نقص در سیستم احراز هویت (Auth Bypass)
در حال حاضر سیستم احراز هویت (`SecurityHelper`) فقط در صفحه `SettleActivity` پیاده‌سازی شده است. این یک نقص امنیتی بزرگ است زیرا:
- **لیست سفارش‌ها (`OrdersActivity`):** هر کسی می‌تواند تمام تاریخچه فروش و داده‌های بیزنس را مشاهده کند.
- **ویرایش و حذف (`EditOrderActivity`):** هر کسی می‌تواند بدون رمز عبور، سفارش‌ها را تغییر داده یا حذف کند که منجر به فساد در دیتابیس مالی می‌شود.
- **گزارش‌ها و پرداخت‌ها:** تمامی آمارهای مالی بدون هیچ لایه محافظتی در دسترس هستند.

**راهکار:** انتقال منطق احراز هویت به یک `BaseActivity` یا پیاده‌سازی آن در `Launcher Activity` برای قفل کردن کل اپلیکیشن.

### ب) خروجی داده‌های حساس (Data Leakage)
کلاس `BackupHelper` داده‌های بیزنس را به صورت یک فایل CSV **رمزنگاری نشده** در پوشه عمومی `Downloads` ذخیره می‌کند. 
- هر اپلیکیشن دیگری با دسترسی به فایل‌ها می‌تواند این داده‌ها را سرقت کند.
- فایل‌ها به راحتی توسط کاربر قابل دسترسی و تغییر هستند.

### ج) تنظیمات مانیفست (`AndroidManifest.xml`)
تنظیم `android:allowBackup="true"` اجازه می‌دهد کل دیتابیس و تنظیمات اپلیکیشن از طریق ADB یا بک‌آپ‌های سیستمی استخراج شود. برای یک اپلیکیشن مالی، این باید `false` باشد.

---

## ۲. پایداری و منطق برنامه (Stability & Logic)

### الف) مدیریت تراکنش‌های مالی
در `MoneyCalculator` از `BigDecimal` استفاده شده که عالی است، اما در برخی جاها مبالغ به صورت `Long` پاس داده می‌شوند. باید اطمینان حاصل شود که در محاسبات بسیار بزرگ، سرریز (Overflow) رخ نمی‌دهد (هرچند برای مبالغ تومان فعلاً کافی است).

### ب) نبود تست‌های جامع
تست‌های موجود بسیار محدود هستند. برای یک اپلیکیشن پروداکشن، تست‌های واحد (Unit Tests) برای محاسبات کمیسیون و گزارش‌گیری باید پوشش ۱۰۰٪ داشته باشند.

---

## ۳. زیرساخت و دیپلوی (Infrastructure & Deployment)

### الف) زنجیره CI/CD
گیت‌هاب اکشن‌های فعلی فقط نسخه `Debug` می‌سازند. برای پروداکشن:
- باید نسخه `Release` ساخته شود.
- فرآیند `Signing` (امضای اپلیکیشن) باید در CI/CD ادغام شود.
- تست‌ها و `Lint` باید قبل از بیلد اجرا شوند.

### ب) ProGuard/R8
فایل `proguard-rules.pro` پایه خوبی دارد اما باید برای کاهش حجم اپلیکیشن و سخت‌تر کردن مهندسی معکوس (Obfuscation) بهینه‌تر شود.

---

## اقدامات فوری پیشنهادی (Immediate Action Plan)

1. **اصلاح مانیفست:** غیرفعال کردن بک‌آپ خودکار.
2. **متمرکز کردن Auth:** ایجاد یک لایه امنیتی سراسری برای ورود به اپلیکیشن.
3. **امن‌سازی خروجی:** افزودن هشدار به کاربر در مورد امنیت فایل‌های CSV یا رمزنگاری آن‌ها.
4. **بهبود CI/CD:** ایجاد ورک‌فلو برای بیلد نسخه امضا شده پروداکشن.
