# گزارش کالبدشکافی عمیق و آنالیز بی‌رحمانه (Deep Production Audit)

این گزارش فراتر از بررسی‌های سطحی، به لایه‌های زیرین منطق بیزنس و زیرساخت پروژه «ماهان» نفوذ کرده است.

---

## ۱. تحلیل لایه داده و یکپارچگی مالی (Data Integrity)

### الف) نقص در اتمیک بودن عملیات تسویه (Atomicity Gap)
در فایل `SettleActivity.kt` و `OrderDao.kt` مشاهده شد که عملیات ثبت پرداخت و تسویه سفارش‌ها به صورت جداگانه انجام می‌شود:
1. ابتدا یک رکورد در جدول `payments` درج می‌شود.
2. سپس سفارش‌های تسویه نشده با یک دستور `UPDATE` در جدول `orders` علامت‌گذاری می‌شوند.

**خطر:** اگر اپلیکیشن بین این دو مرحله کرش کند یا بسته شود، پرداخت ثبت شده اما سفارش‌ها همچنان «بدهی» باقی می‌مانند. این منجر به ناهماهنگی مالی (Double Payment risk) می‌شود.
**راهکار:** تمامی عملیات مالی باید در یک `@Transaction` در سطح DAO یا دیتابیس محصور شوند.

### ب) مدل حسابداری مبتنی بر زمان (Timestamp-based Accounting)
سیستم از `MAX(settled_at)` برای تعیین بازه تسویه استفاده می‌کند. 
- اگر ساعت گوشی کاربر تغییر کند یا به عقب برگردد، محاسبات مالی کاملاً به هم می‌ریزد.
- **راهکار:** استفاده از IDهای یکتا برای هر «دوره تسویه» (Settlement Session) به جای تکیه مطلق بر زمان.

---

## ۲. تحلیل امنیت و نفوذ (Security Deep-Dive)

### الف) سطوح دسترسی ناهماهنگ (Inconsistent Auth Coverage)
با وجود ایجاد `BaseAuthActivity` در فاز قبل، بررسی عمیق نشان داد که:
- صفحه `DriverReportActivity` (که شامل داده‌های حساس مالی است) همچنان بدون احراز هویت باز می‌شود.
- امکان ویرایش و حذف هر سفارشی از طریق این گزارش‌ها بدون رمز عبور وجود دارد.

### ب) امنیت کلیدهای امضا (Keystore Security)
قرار دادن فایل `.jks` و رمزهای آن در مخزن گیت‌هاب (حتی اگر خصوصی باشد) بر خلاف اصول امنیت پروداکشن است.
**راهکار:** استفاده از GitHub Secrets و حذف فایل‌های حساس از تاریخچه گیت.

---

## ۳. پایداری و عملکرد (Stability & Performance)

### الف) نشت حافظه در Clock (Handler Leak)
در `HomeActivity.kt` از یک `Handler` برای ساعت استفاده شده است. هرچند در `onDestroy` حذف می‌شود، اما استفاده از `lifecycleScope` با `delay` برای کارهای تکرار شونده در کاتلین استانداردتر و امن‌تر است.

### ب) همزمانی (Concurrency)
دسترسی به دیتابیس از طریق `Flow`ها به خوبی انجام شده، اما در عملیات نوشتن (`Insert/Update`)، هیچ مکانیزمی برای جلوگیری از ثبت همزمان دو سفارش مشابه (Double Submit) در صورت کلیک سریع کاربر وجود ندارد.

---

## ۴. وضعیت بیلد و انتشار (CI/CD Audit)

ورک‌فلوهای فعلی گیت‌هاب (`build_apk.yml`) فقط نسخه **Debug** می‌سازند.
- **خطر:** نسخه Debug شامل کدهای بهینه‌سازی نشده و قابل دیباگ است که امنیت اپلیکیشن را به شدت کاهش می‌دهد.
- **نیاز:** ایجاد یک Pipeline واقعی که نسخه **Signed Release** را با استفاده از R8/ProGuard تولید کند.

---

## چک‌لیست نهایی برای «دیپلوی بی‌رحمانه»:

1. [ ] انتقال تمامی فعالیت‌های مالی به `BaseAuthActivity`.
2. [ ] محصور کردن عملیات تسویه در `@Transaction`.
3. [ ] اصلاح CI/CD برای تولید نسخه Release امضا شده.
4. [ ] پاکسازی Keystore از مخزن و انتقال به Secrets.
5. [ ] افزودن `debounce` به دکمه‌های ثبت سفارش برای جلوگیری از ثبت تکراری.

**نتیجه‌گیری:** پروژه در لایه UI آماده به نظر می‌رسد، اما در لایه «منطق مالی» و «امنیت سراسری» هنوز به سخت‌گیری بیشتری نیاز دارد تا به معنای واقعی کلمه «Production-Ready» باشد.
